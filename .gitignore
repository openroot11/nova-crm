# Dependencias (se reinstalan con npm install)
node_modules/

# Base de datos en vivo y backups (datos reales de clientes, nunca al repo)
app/server/data/
app/server/backups/

# Credenciales de conexion a la base de datos (Supabase), nunca al repo
.env
app/server/.env

# Reportes/exports reales del negocio (facturas, ventas), nunca al repo
reportes mensuales/

# Config local de Claude Code
.claude/

# Config local del editor
.vscode/

# Sistema
Thumbs.db
.DS_Store
